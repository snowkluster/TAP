services:
  nginx:
    restart: always
    container_name: nginx_app
    build:
      context: ./public
      dockerfile: Dockerfile
    network_mode: "host"
    volumes:
      - ./public/nginx.conf:/etc/nginx/nginx.conf:ro

networks:
  my_network:
    driver: bridge
